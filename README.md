# Basic Flight Tracker App
Flight tracking app built with the Aviationstack API

## Things I learned
- Used a forEach array method to iterate through the returned JSON object results to print the top 100 to the screen
